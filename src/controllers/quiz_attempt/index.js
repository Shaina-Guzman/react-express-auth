const list = require('./list');
const create = require('./create');
const find = require('./find');
const update = require('./update');
const getAverageQuizScore = require('./getAverageQuizScore');

module.exports = {
  list,
  create,
  find,
  update,
  getAverageQuizScore,
};
